pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Jenkins from git hub'
            }
        }
        stage('World') {
            steps {
                echo 'Hello Jenkins from git hub again'
            }
        }
    }
}
